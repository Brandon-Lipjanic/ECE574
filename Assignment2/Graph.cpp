#include "Graph.h"
#include <string>
#include <iostream>
#include <string.h>
#include <vector>
#include <cstdio>
#include "Node.h"
#include "Edge.h"
#define ADD1 2.704


void Graph::parseString(std::string tempStr){

	vector<Edges>

	if (tempStr.find('+') != std::string::npos && tempStr.find("+ 1") == std::string::npos) {
		

	}

}
